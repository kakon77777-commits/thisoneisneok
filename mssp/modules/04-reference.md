---
id: reference
index: "04"
title_zh: 參考實作
title_en: Reference implementations
summary_zh: 可執行的範例。每個只示範一個結構決策，並在建置時被機械檢查。目前 1 個。
summary_en: Runnable examples, each demonstrating one structural decision and checked mechanically at build time. One so far.
state_zh: 進行中
state_en: In progress
updated: 2026-07-31
---

# 參考實作

## 目前狀態：1 個範例

- [001 — 任務執行器：兩個 TMS 想互相引用時](/html/mssp/001-task-runner.html)

節奏是**一天一個**。累積速度會比一次寫一份大文件慢，但每一個都是可以打開、可以執行、可以被反駁的東西。

## 每個範例必須成立的四件事

這不是編輯建議，是 `scripts/build-mssp.mjs` 強制的契約。任何一項失敗，範例不會被發佈——而不是發佈後在頁面上標註警告。

**一、它真的會跑。** `meta.yaml` 裡的 `runnable` 指令會被執行，非零離開碼直接讓 build 失敗。

**二、沒有任何 TMS 引用兄弟 TMS。** 機械檢查，不靠人眼。一個 TMS 單元的定義是「帶有 index 檔的目錄，否則就是單一檔案」——所以放在同一個分類目錄下的兩個檔案是**兩個**單元，它們之間的 import 是違規。

**三、沒有空的集合目錄。** 一個空的 `TMS/` 什麼也沒教，卻暗示了不存在的結構。

**四、README 必須寫出「這個範例沒有解決什麼」。** 沒有這一節，build 失敗。

## 一個範例只示範一個決策

示範四件事的範例，一件也沒示範成。

好的題目長這樣：

- 把某個能力從 TMS 升到 SMS，以及升上去之後什麼壞了
- 兩個 TMS 想互相引用，SMS 契約如何解決（001 號）
- 一個 runtime 真的能檢查的 SCL 權限
- 一個把「執行成功」變成人類可驗證的 DMS 視圖
- 同一支程式重構前後，耦合量測的差別

不好的題目：「MSSP 應用於 X」而沒有具體決策，或者重寫一個大型框架。

## 反例是合法的範例

契約支援 `kind: counterexample`。刻意示範錯誤結構的範例會被網站標上警語，讓沒有人會照抄。

這一類其實比正面範例更需要：多數人不是不知道好結構長什麼樣，而是分不出自己手上的東西屬於哪一種。

## 誠實規範

每個範例的 README 有一節是強制的，內容也必須是真的。001 號的那一節寫著：

> 306 行程式碼（含註解 496 行）分散在 13 個檔案，如果寫成兩個檔案會更好讀。五個集合放在這裡是為了讓各部分可見，不是因為這個規模需要它們。把一個過度結構化的玩具當成方法有效的證據，是不誠實的。

**在小規模上使用完整 MSSP 是有代價的，而且這個代價是方法自己承認的。** 一個範例可以選擇用小問題來讓結構可見，但必須說出來。

## 路線

從小型可運行範例，逐步推進到遊戲運行框架、Agent 與世界狀態系統。這個順序不是難度排序，是**驗證強度**排序：小範例能被完整檢查，大系統只能被抽樣。

在能被完整檢查的規模上先把方法弄對，比在無法驗證的規模上宣稱它有效重要。

## 還沒有做的

- Router 的實作。001 號沒有 Router，因為在一個型別對一個處理器的情況下，路由就是一次查表，另設一層會是空模組。
- 版本與遷移。完整 MSSP-Scale 的七個部分裡有三個在 001 號中完全缺席。
- 多語言。目前只有 JavaScript。契約本身不綁語言，但「TMS 不引用兄弟 TMS」的檢查目前只認得靜態 `import`。
