# Architect opinions

Use `YYYY-MM-DD-<speaker>-<topic>.md`. Each file binds one speaker to a host-observed instance when available and preserves dissent. An architect may propose, object, or withdraw only their own position.

Opinions are not decisions. Promote a result to `decisions/` only after the required consensus or Neo authorization is independently recorded.
