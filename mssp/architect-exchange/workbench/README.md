# Workbench packages

One task per directory:

```text
YYYY-MM-DD-<task>/
  REQUEST.md
  INPUTS.md
  RESULT.md
  artifacts/
```

`REQUEST.md` is the implementation authority for that task. `INPUTS.md` pins exact sources and hashes. `RESULT.md` is a delivery description, not acceptance evidence.
